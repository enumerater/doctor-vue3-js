# 小农智慧农业前端项目代码总结

> 项目名称：doctor-vue3-js（小农智慧农业助手）
> 技术栈：Vue 3 + Vite + Pinia + Vue Router + Element Plus + Vant + ECharts + Three.js

---

## 一、项目概述

本项目是一个**智慧农业助手**前端应用，面向农业用户提供智能对话、图像病害诊断、农田地块管理、病害知识库、数字孪生看板等核心功能，并配套管理后台。系统同时支持 PC 端与移动端（响应式布局）。

---

## 二、技术栈与依赖

### 核心框架
| 依赖 | 版本 | 用途 |
|------|------|------|
| Vue | ^3.5.18 | 前端框架 |
| Vite | ^7.0.6 | 构建工具 |
| Vue Router | ^4.5.1 | 路由管理 |
| Pinia | ^3.0.3 | 状态管理 |
| pinia-plugin-persistedstate | ^4.7.1 | 状态持久化 |

### UI 组件库
| 依赖 | 版本 | 用途 |
|------|------|------|
| Element Plus | ^2.13.2 | PC 端 UI 组件 |
| Vant | ^4.9.21 | 移动端 UI 组件 |
| @element-plus/icons-vue | ^2.3.2 | 图标库 |

### 数据可视化与 3D
| 依赖 | 版本 | 用途 |
|------|------|------|
| ECharts | ^5.6.0 | 图表可视化 |
| Three.js | ^0.170.0 | 3D 渲染（数字孪生） |
| d3 | ^7.9.0 | 数据可视化 |
| gsap | ^3.12.5 | 动画库 |
| @amap/amap-jsapi-loader | ^1.0.1 | 高德地图 |

### 其他工具
| 依赖 | 版本 | 用途 |
|------|------|------|
| axios | ^1.13.2 | HTTP 请求 |
| crypto-js | ^4.2.0 | 加密 |
| dayjs | ^1.11.19 | 日期处理 |
| marked | ^17.0.1 | Markdown 渲染 |
| vue3-markdown-it | ^1.0.10 | Markdown 渲染 |
| vuedraggable | ^4.1.0 | 拖拽排序 |

---

## 三、项目目录结构

```
doctor-vue3-js/
├── src/
│   ├── main.js              # 应用入口
│   ├── App.vue              # 根组件
│   ├── router/              # 路由配置
│   ├── stores/              # Pinia 状态管理（17个）
│   ├── axios/               # API 请求封装（17个）
│   ├── views/               # 页面视图（11个模块）
│   ├── components/          # 通用组件
│   ├── layouts/             # 布局组件
│   ├── constants/           # 常量定义
│   ├── utils/               # 工具函数
│   └── styles/              # 全局样式
├── vite.config.js           # Vite 配置
└── package.json
```

---

## 四、应用入口与配置

### 4.1 main.js（应用入口）
- 创建 Vue 应用，注册 Pinia（含持久化插件）、Router
- 全局注册 Element Plus 和 Vant 组件
- 将 ECharts 挂载到 `window.echarts` 全局使用
- 挂载后初始化用户 Store

### 4.2 vite.config.js（构建配置）
- **base**: `/nong/`（部署路径前缀）
- **开发代理**：
  - `/api/java` → `http://localhost:8080`（后端 HTTP）
  - `/api/java/ws/chat` → WebSocket 聊天
  - `/api/java/ws/agent` → WebSocket Agent
- **自动导入**：Element Plus 组件按需自动导入
- **SCSS 全局变量**：自动注入 `variables.scss`
- **别名**：`@` → `src`

### 4.3 App.vue（根组件）
- 仅包含 `<router-view>`，引入全局样式（element-theme、global、animations）

---

## 五、路由设计（router/index.js）

### 5.1 路由结构
采用**嵌套路由**，主布局为 `AppShell`，管理后台为 `AdminLayout`。

| 路由 | 名称 | 页面 | 说明 |
|------|------|------|------|
| `/login` | login | LoginView | 登录页（无布局） |
| `/workbench` | workbench | WorkbenchView | 工作台首页 |
| `/chat` | chatHome | ChatHome | 对话首页 |
| `/chat/:sessionId` | chatDetail | ChatDetail | 对话详情 |
| `/vision` | vision | VisionView | 智能图像诊断 |
| `/diagnosis/:diagnosisId` | diagnosisDetail | DiagnosisDetail | 诊断详情 |
| `/report/:diagnosisId` | reportPreview | ReportPreview | 诊断报告 |
| `/knowledge` | knowledge | KnowledgeIndex | 病害知识库 |
| `/knowledge/search` | knowledgeSearch | KnowledgeSearch | 搜索病害 |
| `/farm` | farm | FarmIndex | 我的农场 |
| `/farm/twin/:farmId?` | farmTwin | FarmTwin | 数字孪生看板 |
| `/farm/create` | farmCreate | FarmCreate | 创建农场 |
| `/farm/:farmId` | farmDetail | FarmDetail | 农场详情 |
| `/farm/:farmId/plot/create` | plotCreate | PlotCreate | 添加地块 |
| `/farm/:farmId/plot/:plotId` | plotDetail | PlotDetail | 地块详情 |
| `/farm/:farmId/plot/:plotId/accumulated-temp` | accumulatedTemp | AccumulatedTempView | 积温预测 |
| `/farm/:farmId/plot/:plotId/records` | plotRecords | PlotRecordsView | 农事记录 |
| `/farm/:farmId/plot/:plotId/pesticide/create` | pesticideCreate | PesticideCreate | 记录施药 |
| `/farm/:farmId/plot/:plotId/field-note/create` | fieldNoteCreate | FieldNoteCreate | 记随笔 |
| `/notifications` | notifications | NotificationIndex | 通知中心 |
| `/settings` | settings | UserSettings | 个人设置 |
| `/admin` | adminDashboard | AdminDashboard | 管理后台-系统概览 |
| `/admin/users` | adminUsers | AdminUsers | 用户管理 |
| `/admin/knowledge` | adminKnowledge | AdminKnowledge | 知识库管理 |
| `/admin/feedback` | adminFeedback | AdminFeedback | 反馈审核 |
| `/admin/announcement` | adminAnnouncement | AdminAnnouncement | 公告推送 |
| `/admin/logs` | adminLogs | AdminLog | 任务日志 |

### 5.2 路由守卫
- **登录校验**：无 token 且非白名单（`/login`）时重定向到登录页
- **管理员权限**：访问 `/admin` 且 `role !== '1'` 时重定向到工作台
- **页面标题**：自动设置为 `小农-{title}`
- **旧路由兼容**：`/home/*` 系列重定向到新路径

---

## 六、状态管理（stores/，共17个）

| Store | 职责 |
|-------|------|
| `user.js` | 用户信息、token 管理、登录/登出、token 过期检测、管理员判断 |
| `chat.js` | 对话消息、SSE 流式对话、会话管理、模型选择 |
| `agent.js` | Agent WebSocket 连接、消息收发、工具调用步骤、思考过程、确认/提问交互 |
| `sidebar.js` | 侧边栏折叠、对话历史、搜索、Agent 切换 |
| `vision.js` | 图像识别任务状态、历史记录（持久化） |
| `diagnosis.js` | 诊断记录、统计、分页筛选 |
| `farm.js` | 农场/地块 CRUD、生长阶段管理 |
| `farm_records.js` | 农事记录 |
| `farm_twin.js` | 数字孪生数据 |
| `knowledge.js` | 病害知识库 |
| `feedback.js` | 用户反馈 |
| `notification.js` | 通知中心 |
| `prediction.js` | 积温预测 |
| `plot_diagnosis.js` | 地块诊断 |
| `skills.js` | Agent 技能 |
| `admin.js` | 管理后台数据 |
| `net.js` | 网络状态 |

### 关键 Store 详解

**user.js**：管理用户 token、用户名、角色（0普通/1管理员）、状态，提供 `setUser`、`initUser`、`logout`、`updateProfile`，token 24 小时过期，数据同步到 localStorage。

**chat.js**：管理对话消息列表，支持 SSE 流式接收（`startStreaming`），通过 `fetch` 读取 `text/event-stream` 流，逐块追加机器人回复。

**agent.js**：核心 Agent 状态管理，通过 WebSocket 与后端通信，管理连接状态、消息列表、当前回合工具步骤、思考文本、待确认/待提问交互，支持断线重连（最多5次）和心跳检测。

---

## 七、API 请求层（axios/，共17个）

| 模块 | 主要接口 |
|------|---------|
| `user.js` | 登录、邮箱验证码登录、更新资料、改密码、上传头像 |
| `chat.js` | 记忆对话、获取消息、保存 Agent 消息、图像识别 |
| `session.js` | 会话创建 |
| `farm.js` | 农场/地块 CRUD、生长阶段（含 Mock 数据兜底） |
| `farm_records.js` | 农事记录 |
| `farm_twin.js` | 数字孪生 |
| `diagnosis.js` | 诊断记录、统计 |
| `knowledge.js` | 知识库 |
| `feedback.js` | 反馈 |
| `notification.js` | 通知 |
| `prediction.js` | 积温预测 |
| `plot_diagnosis.js` | 地块诊断 |
| `skills.js` | Agent 技能 |
| `oss.js` | 文件上传 |
| `admin.js` | 管理后台 |
| `announcement.js` | 公告 |
| `data.js` | 数据 |

### 请求封装（utils/requests.js）
- 基于 axios 实例，baseURL 从环境变量读取
- **请求拦截器**：检查 token 过期，自动携带 Authorization
- **响应拦截器**：业务错误码（code !== 200）拦截、401 自动登出

---

## 八、页面视图（views/，11个模块）

### 8.1 登录页（LoginView.vue）
- 密码登录 + 邮箱验证码登录两种方式
- 装饰性背景（SVG 叶子动画）

### 8.2 工作台（WorkbenchView.vue）
- 欢迎横幅、快捷对话卡片、快捷诊断卡片、功能宫格、最近活动

### 8.3 对话模块（chat/）
- **ChatLayout**：PC 侧边栏 + 移动端全屏历史页
- **ChatHome**：空状态（Logo + 热门问题）与消息列表
- **ChatDetail**：完整对话详情，支持 Agent 转移弹窗、Markdown 渲染、工具步骤展示

### 8.4 图像诊断（VisionView.vue）
- 上传图片进行病害识别，展示识别结果与历史记录

### 8.5 农田管理（farm/，13个文件）
- 农场列表、创建/编辑、详情
- 地块创建/编辑/详情、生长阶段管理
- 积温预测、农事记录、施药记录、田间随笔
- 数字孪生看板（FarmTwin）

### 8.6 知识库（knowledge/）
- 病害知识库索引、病害搜索

### 8.7 管理后台（admin/，7个文件）
- AdminLayout 布局 + 系统概览、用户管理、知识库管理、反馈审核、公告推送、任务日志

### 8.8 其他
- 诊断详情、报告预览、通知中心、个人设置、天气、数据大屏、农事日历

---

## 九、布局组件（layouts/）

### AppShell.vue（主布局）
- 顶部导航（TopHeader）+ 侧边导航（SideNav）+ 主内容区
- 移动端（≤768px）自动切换为底部 TabBar（MobileTabBar）
- 支持侧边栏折叠、页面切换淡入淡出动画

---

## 十、通用组件（components/）

### 布局组件（layout/）
- `TopHeader.vue`：顶部导航
- `SideNav.vue`：侧边导航
- `MobileTabBar.vue`：移动端底部导航
- `ContentHeader.vue`：内容页头部

### 工作台组件（workbench/）
- `WelcomeBanner.vue`、`QuickChatCard.vue`、`QuickVisionCard.vue`、`FeatureGrid.vue`、`RecentActivity.vue`

### 对话组件
- `ChatInputCard.vue`：对话输入框
- `AgentTransferPopup.vue`：Agent 转移弹窗
- `AgentHotQuestionCard.vue` / `HotQuestionCard.vue`：热门问题卡片
- `HistoryList.vue`：对话历史列表
- `SkillsBar.vue`：技能栏

### 其他
- `LogoSection.vue`：Logo 展示
- `MobileComponent.vue` / `PasswordComponent.vue`：登录组件
- 诊断、农场、反馈、知识库、孪生等模块组件

---

## 十一、Agent 通信协议（constants/agentProtocol.js）

### 消息类型（MSG_TYPE）
- **前端 → 后端**：`user_input`、`user_confirm`、`user_answer`、`heartbeat`
- **后端 → 前端**：`thought`、`thought_result`、`tool_call`、`tool_result`、`confirm`、`ask`、`answer`、`error`、`heartbeat_pong`

### 工具配置（TOOL_CONFIG）
| 工具 | 名称 | 颜色 |
|------|------|------|
| knowledge_base | 知识库检索 | #3B82F6 |
| knowledge_graph | 知识图谱 | #8B5CF6 |
| memory_retrieval | 记忆检索 | #EC4899 |
| web_search | 联网搜索 | #06B6D4 |
| weather | 天气查询 | #F59E0B |
| image_recognition | 图像识别 | #10B981 |
| field_management | 田间管理 | #84CC16 |
| medication | 施药方案 | #EF4444 |
| accumulated_temp | 积温预测 | #F97316 |

---

## 十二、样式体系（styles/）

| 文件 | 用途 |
|------|------|
| `variables.scss` | SCSS 全局变量（颜色、尺寸） |
| `mixins.scss` | 混入（如移动端适配） |
| `global.scss` | 全局样式 |
| `element-theme.scss` | Element Plus 主题定制 |
| `animations.scss` | 动画 |
| `markdown.scss` | Markdown 渲染样式 |

---

## 十三、核心功能流程

### 13.1 智能对话（SSE 流式）
1. 用户输入消息 → `chatStore.sendMessage()`
2. `prepareMessage()` 准备消息并创建会话
3. `startStreaming()` 通过 `fetch` 读取 SSE 流
4. 逐块解析 `data:` 前缀内容，实时追加到机器人消息

### 13.2 Agent 智能体（WebSocket）
1. `agentStore.connect()` 建立 WebSocket 连接（带 token、userId、sessionId）
2. 后端推送 `thought`（思考）、`tool_call`（工具调用）、`tool_result`（工具结果）
3. 前端实时展示思考文本和工具执行步骤
4. 遇到 `confirm`/`ask` 时暂停等待用户确认/回答
5. 支持断线重连（最多5次）和心跳保活

### 13.3 图像病害诊断
1. 用户上传图片 → `visionStore.createTask()`
2. 调用 `/vision` 接口（60秒超时）
3. 展示识别结果，保存到历史记录（最多10条，持久化）

### 13.4 农田管理
- 农场/地块的增删改查，生长阶段管理
- 农事记录、施药记录、田间随笔
- 积温预测、数字孪生看板

---

## 十四、权限控制

| 角色 | 权限 |
|------|------|
| 普通用户（role=0） | 访问所有业务功能，不能访问管理后台 |
| 管理员（role=1） | 额外可访问 `/admin` 管理后台 |

权限通过路由守卫 + localStorage 中的 `role` 字段控制。

---

## 十五、开发命令

```bash
npm run dev       # 启动开发服务器（--host）
npm run build     # 生产构建
npm run preview   # 预览构建产物
npm run lint      # ESLint 检查并修复
npm run format    # Prettier 格式化
```

---

## 十六、环境与部署

- **Node 版本**：^20.19.0 或 >=22.12.0
- **部署路径**：`/nong/`（vite base 配置）
- **后端地址**：通过环境变量 `VITE_APP_BASE_API` 配置，开发环境代理到 `localhost:8080`
- **WebSocket**：`/ws/chat`（对话）、`/ws/agent`（Agent）
