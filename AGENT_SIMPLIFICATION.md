# Agent配置简化方案 - 设计说明

## 简化原则

> **核心理念**：给农民用的系统，要像微信聊天一样简单！

---

## 🎯 简化前 vs 简化后

### 简化前（太复杂❌）

```
┌─────────────────────────────────────┐
│      Agent 配置管理                  │
├─────────────────────────────────────┤
│ [配置列表] [编辑配置] [新建配置]      │
├─────────────────────────────────────┤
│ 1. 基本信息                          │
│    - 配置名称                        │
│    - 配置描述                        │
│    - 是否默认                        │
│                                     │
│ 2. 系统提示词（5个预设模板）          │
│    - 作物病害诊断专家                │
│    - 田间管理顾问                    │
│    - 植保用药专家                    │
│    - 综合农技服务                    │
│    - 精准农业数据分析                │
│    - 自定义补充指令                  │
│    - 语言选择                        │
│                                     │
│ 3. 响应参数（农民看不懂❌）           │
│    - 回答风格（温度）                │
│      • 精准(0.3) / 平衡(0.7) / 创意(0.9) │
│    - 回答长度（maxTokens）           │
│      • 简洁(1000) / 适中(2000) / 详细(4000) │
│    - 高级参数：                      │
│      • topP (0.0-1.0)               │
│      • topK (1-100)                 │
│      • frequencyPenalty (-2.0-2.0)  │
│      • presencePenalty (-2.0-2.0)   │
│                                     │
│ 4. Agent业务参数                     │
│    - 农作物类型（16种，太多）         │
│    - 诊断模式：快速/平衡/精准         │
│    - 功能开关：3个                   │
│    - 最大步骤数：3-10步              │
│    - 输出格式：简洁/详细/技术         │
│                                     │
│ 5. 高级配置                          │
│    - 缓存开关                        │
│    - 缓存过期时间                    │
│    - 重试机制                        │
│    - 最大重试次数                    │
│    - 重试延迟                        │
│    - 超时设置                        │
└─────────────────────────────────────┘
```

**问题**：
- ❌ 3个标签页切来切去，农民晕了
- ❌ temperature、topP、topK 这些是啥？
- ❌ 16种作物太多，农民只种几样
- ❌ 高级配置（缓存、重试）农民根本不懂
- ❌ 需要新建、编辑、管理多个配置，太麻烦

---

### 简化后（简单明了✅）

```
┌─────────────────────────────────────┐
│         Agent 设置                   │
│                               [关闭] │
├─────────────────────────────────────┤
│                                     │
│ 📍 农业先验知识                      │
│ └─ 告诉我一些基本信息，我会给你更准确的建议 │
│                                     │
│ 你在哪里种地？                       │
│ [省份（如：山东省）____________]      │
│ [城市（如：潍坊市）____________]      │
│                                     │
│ 你种什么作物？                       │
│ ☑ 玉米    ☑ 小麦    □ 水稻    □ 番茄 │
│ □ 黄瓜    □ 辣椒    □ 苹果    □ 葡萄 │
│                                     │
│ 现在是什么阶段？                     │
│ ○ 播种期  ● 生长期  ○ 开花期         │
│ ○ 结果期  ○ 成熟期                  │
│                                     │
├─────────────────────────────────────┤
│ 🛠️ 需要什么帮助？                   │
│ └─ 选择你需要的服务                  │
│                                     │
│ [图片诊断          ] [开] │
│  上传作物照片，帮你看病               │
│                                     │
│ [管理建议          ] [开] │
│  施肥、浇水、除草等日常管理           │
│                                     │
│ [用药方案          ] [开] │
│  推荐农药和使用方法                  │
│                                     │
├─────────────────────────────────────┤
│ ⚙️ 高级选项 ▼（展开）                │
│                                     │
│ 智能体提示词（选填）                 │
│ 给专业用户：自定义Agent的行为规则     │
│ ┌─────────────────────────────────┐ │
│ │例如：你是一个专注于病虫害防治的   │ │
│ │专家，回答要简洁明了...            │ │
│ │                               │ │
│ │                               │ │
│ └─────────────────────────────────┘ │
│                                     │
├─────────────────────────────────────┤
│ 🔥 Skills 扩展功能即将上线            │
│    未来可接入更多专业技能模块         │
│                                     │
├─────────────────────────────────────┤
│       [保存设置]                     │
└─────────────────────────────────────┘
```

**改进**：
- ✅ 单页面配置，不需要切换标签
- ✅ 用大白话（"你在哪里种地"而不是"地域配置"）
- ✅ 只保留8个常见作物
- ✅ 新增地域和生长阶段（真正的先验知识）
- ✅ 功能开关用大白话（"图片诊断"而不是"enableImageAnalysis"）
- ✅ 高级选项折叠隐藏，不干扰普通用户
- ✅ 预留Skills扩展接口

---

## 🗑️ 删除的复杂配置

### 1. AI底层参数（农民看不懂）

| 参数 | 说明 | 为什么删除 |
|------|------|------------|
| `temperature` | 控制随机性 | 农民不知道什么是"温度"，系统自动用0.7即可 |
| `topP` | 核采样阈值 | 技术参数，农民完全不懂 |
| `topK` | 候选token数量 | 技术参数，农民完全不懂 |
| `frequencyPenalty` | 频率惩罚 | 技术参数，农民完全不懂 |
| `presencePenalty` | 存在惩罚 | 技术参数，农民完全不懂 |
| `maxTokens` | 最大token数 | 农民不知道什么是token，系统自动调整 |

**替代方案**：后端使用固定的合理默认值（temperature=0.7, maxTokens=2500）

### 2. 配置管理功能（太复杂）

| 功能 | 说明 | 为什么删除 |
|------|------|------------|
| 配置列表 | 管理多个配置 | 农民只需要一个配置就够了 |
| 新建配置 | 创建新配置 | 不需要多个配置，一个就行 |
| 编辑配置 | 修改现有配置 | 直接在主界面改，不需要单独的编辑页 |
| 复制配置 | 复制配置 | 不需要多个配置，所以也不需要复制 |
| 设为默认 | 标记默认配置 | 只有一个配置，不需要默认的概念 |
| 删除配置 | 删除配置 | 只有一个配置，不能删除 |

**替代方案**：单一配置，直接修改保存

### 3. 系统提示词预设模板（太复杂）

删除的5个预设模板：
- ❌ 作物病害诊断专家
- ❌ 田间管理顾问
- ❌ 植保用药专家
- ❌ 综合农技服务
- ❌ 精准农业数据分析

**替代方案**：
- 后端使用统一的默认提示词
- 专业用户可以在"高级选项"中自定义

### 4. 诊断模式选择（不需要）

删除的选项：
- ❌ 快速模式
- ❌ 平衡模式
- ❌ 精准模式

**替代方案**：系统根据问题复杂度自动调整

### 5. 其他复杂参数

| 参数 | 为什么删除 |
|------|------------|
| 最大步骤数（3-10步） | 农民不懂什么是"推理步骤"，系统自动控制 |
| 输出格式（简洁/详细/技术） | 系统根据问题自动调整详细程度 |
| 缓存配置 | 技术细节，农民不需要关心 |
| 重试机制 | 技术细节，农民不需要关心 |
| 超时设置 | 技术细节，农民不需要关心 |

---

## ✅ 保留的核心配置

### 1. 农业先验知识（新增✨）

这些是真正有用的先验知识，能让Agent给出更准确的建议：

```javascript
{
  province: '山东省',        // 新增：省份（影响气候、病害类型）
  city: '潍坊市',           // 新增：城市（更精确的地域信息）
  cropTypes: ['corn', 'wheat'],  // 作物类型（简化为8个常见作物）
  growthStage: 'growing'    // 新增：生长阶段（不同阶段病害不同）
}
```

**为什么重要**：
- 地域影响：不同地区气候不同，病害类型不同
- 作物类型：针对性给出建议
- 生长阶段：同样的病害在不同阶段处理方式不同

### 2. 功能开关（简化）

```javascript
{
  enableImageAnalysis: true,      // 图片诊断
  enableFieldManagement: true,    // 管理建议
  enablePesticideAdvice: true     // 用药方案
}
```

**改进**：
- 用大白话命名（"图片诊断"而不是"enableImageAnalysis"）
- 每个开关都有说明文字

### 3. 智能体提示词（可选）

```javascript
{
  customPrompt: '你是一个专注于病虫害防治的专家...'
}
```

**改进**：
- 折叠在"高级选项"中，不干扰普通用户
- 只有一个简单的文本框，不需要复杂的模板选择
- 专业用户可以自定义，普通用户可以忽略

### 4. Skills扩展接口（预留）

```javascript
{
  enabledSkills: []  // 预留：未来可以接入更多skills
}
```

**未来计划**：
- 病害识别skill
- 施肥计算skill
- 天气预测skill
- 价格预测skill

---

## 📊 数据对比

| 项目 | 简化前 | 简化后 | 减少 |
|------|--------|--------|------|
| 配置页面数量 | 3个标签页 | 1个单页 | 66% |
| 配置项数量 | 23个 | 8个 | 65% |
| 技术参数数量 | 11个 | 0个 | 100% |
| 作物选项数量 | 16个 | 8个 | 50% |
| 配置管理操作 | 6个 | 1个 | 83% |

---

## 🎨 UI/UX改进

### 1. 语言简化

| 简化前（技术术语） | 简化后（大白话） |
|-------------------|-----------------|
| "响应参数配置" | "需要什么帮助？" |
| "temperature控制随机性" | （删除，系统自动） |
| "enableImageAnalysis" | "图片诊断 - 上传作物照片，帮你看病" |
| "diseaseDetectionMode" | （删除，系统自动） |
| "Agent业务参数" | "农业先验知识" |
| "cropTypes配置" | "你种什么作物？" |

### 2. 界面简化

- ✅ 单页面，不需要切换标签
- ✅ 清晰的分区（先验知识 → 功能开关 → 高级选项 → Skills）
- ✅ 每个配置都有易懂的说明文字
- ✅ 高级选项折叠隐藏
- ✅ Skills预留区域有明确的提示

---

## 🔧 技术实现

### 修改的文件

1. **AgentConfigPanel.vue**
   - 删除三个标签页
   - 改为单页面加载 SimpleAgentConfig

2. **SimpleAgentConfig.vue**（新建）
   - 简化的配置界面
   - 农业先验知识配置
   - 功能开关
   - 高级选项（折叠）
   - Skills预留区域

### 保留的文件（向后兼容）

- `stores/agentConfig.js` - 状态管理（不变）
- `axios/agentConfig.js` - API接口（不变）

### 删除的文件（可选）

如果要彻底清理，可以删除：
- `ConfigSelector.vue` - 配置列表
- `ConfigEditor.vue` - 配置编辑
- `ConfigCreator.vue` - 配置创建
- `SystemPromptSection.vue` - 复杂的提示词配置
- `ResponseParamSection.vue` - AI底层参数配置
- `AdvancedConfigSection.vue` - 高级配置

**注意**：暂时保留这些文件，以防需要回滚

---

## 📝 后端需要的改动

### 数据库字段变化

```sql
-- 新增字段
ALTER TABLE agent_configs ADD COLUMN province VARCHAR(50);
ALTER TABLE agent_configs ADD COLUMN city VARCHAR(50);
ALTER TABLE agent_configs ADD COLUMN growth_stage VARCHAR(20);
ALTER TABLE agent_configs ADD COLUMN custom_prompt TEXT;

-- 简化字段（可选：保留但前端不再使用）
-- temperature, topP, topK, frequencyPenalty, presencePenalty
-- diseaseDetectionMode, maxSteps, outputFormat
```

### API调整

**不需要修改API接口**，只需要：
1. 接受新的字段（province, city, growthStage）
2. 对于缺失的AI参数，使用默认值：
   ```javascript
   {
     temperature: 0.7,
     maxTokens: 2500,
     topP: 0.9,
     topK: 40,
     frequencyPenalty: 0,
     presencePenalty: 0
   }
   ```

---

## 🚀 上线计划

### Phase 1: 前端简化（已完成✅）
- [x] 简化配置界面
- [x] 新增地域和生长阶段配置
- [x] 删除所有技术参数
- [x] 预留Skills扩展接口

### Phase 2: 后端适配（待完成）
- [ ] 数据库字段迁移
- [ ] API接口适配新字段
- [ ] 使用默认AI参数
- [ ] 测试验证

### Phase 3: 数据迁移（可选）
- [ ] 迁移现有配置数据
- [ ] 清理无用的旧配置

### Phase 4: 清理旧代码（可选）
- [ ] 删除旧的配置组件
- [ ] 更新文档

---

## 💡 使用示例

### 农民老王的使用场景

**简化前**：
1. 打开Agent设置
2. 看到"配置列表"、"编辑配置"、"新建配置"三个标签，不知道点哪个
3. 点开"新建配置"，看到一堆配置项
4. temperature是啥？topP是啥？完全看不懂
5. 算了，不配置了，直接用吧

**简化后**：
1. 打开Agent设置
2. "你在哪里种地？" → 填"山东省 潍坊市"
3. "你种什么作物？" → 勾选"玉米"和"小麦"
4. "现在是什么阶段？" → 选择"生长期"
5. 点"保存设置"，完成！

---

## 📞 反馈

如果有任何问题或建议，请联系开发团队。

---

**最后更新**：2026-02-09
**版本**：v2.0-simplified
