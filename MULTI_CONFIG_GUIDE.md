# 多配置管理功能说明

## 📚 概述

虽然简化了配置界面,但保留了完整的多配置管理功能,用户可以创建和管理多套Agent配置。

## 🎯 使用场景

### 为什么需要多配置?

1. **不同作物**: 为玉米、小麦等不同作物创建专用配置
2. **不同地块**: 为不同地区的农田创建配置
3. **不同阶段**: 为播种期、生长期等不同阶段创建配置
4. **不同用途**: 诊断配置、管理配置、查询配置等

## 🚀 快速开始

### 1. 查看当前配置

打开Agent设置,顶部会显示当前使用的配置:

```
┌─────────────────────────────────────┐
│ 当前配置                             │
│ ┌───────────────────────────────┐   │
│ │ [玉米专用配置 ▼]        [⚙️管理]│   │
│ └───────────────────────────────┘   │
└─────────────────────────────────────┘
```

### 2. 切换配置

点击配置名称下拉框,选择要使用的配置:

```
┌─────────────────────────────────────┐
│ 选择配置                             │
├─────────────────────────────────────┤
│ ○ 默认配置                           │
│ ● 玉米专用配置                       │
│ ○ 小麦专用配置                       │
│ ○ 诊断配置                           │
└─────────────────────────────────────┘
```

### 3. 管理配置

点击"管理"按钮,打开配置管理弹窗:

```
┌─────────────────────────────────────┐
│ 配置管理                        [✕] │
├─────────────────────────────────────┤
│ ⭐ 默认配置                          │
│    作物:2种 | 山东省 潍坊市          │
│    [编辑] [复制] [删除]              │
├─────────────────────────────────────┤
│ ⭐ 玉米专用配置     [当前使用]       │
│    作物:1种 | 山东省 潍坊市          │
│    [切换] [编辑] [复制] [删除]       │
├─────────────────────────────────────┤
│ ☆ 小麦专用配置                       │
│    作物:1种 | 山东省 潍坊市          │
│    [切换] [编辑] [复制] [删除]       │
├─────────────────────────────────────┤
│          [+ 新建配置]                │
└─────────────────────────────────────┘
```

## 🔧 配置操作

### 1. 新建配置

点击"新建配置"按钮:

1. 输入配置名称(如"玉米专用配置")
2. 输入配置说明(可选)
3. 点击确认

新配置会复制当前配置的内容,你可以在此基础上修改。

### 2. 重命名配置

点击配置的"编辑"图标:

1. 输入新的配置名称
2. 点击确认

### 3. 复制配置

点击配置的"复制"图标:

系统会自动创建一个副本,名称为"原配置名 - 副本"。

### 4. 删除配置

点击配置的"删除"图标:

1. 确认删除操作
2. 点击确定

**注意**: 至少保留一个配置,最后一个配置无法删除。

### 5. 切换配置

点击配置的"切换"按钮:

立即切换到该配置,配置面板会自动刷新为该配置的内容。

## 📋 配置内容

每个配置包含以下内容:

### 基本信息
- 配置名称
- 配置说明
- 是否为默认配置

### 农业先验知识
- 省份
- 城市
- 种植作物
- 生长阶段

### 功能开关
- 图片诊断
- 管理建议
- 用药方案

### 高级选项
- 智能体提示词

### Skills配置
- 启用的Skills列表

## 💡 使用技巧

### 1. 为不同作物创建配置

**场景**: 你种植玉米和小麦,需要分别管理

**操作**:
1. 创建"玉米配置"
   - 作物: 只选择玉米
   - 启用: 病害识别、施肥计算、天气预报
2. 创建"小麦配置"
   - 作物: 只选择小麦
   - 启用: 病害识别、施肥计算、价格查询

**好处**:
- Agent会针对特定作物给出更精准的建议
- Skills推荐也更加相关

### 2. 为不同地块创建配置

**场景**: 你在两个不同的地方有农田

**操作**:
1. 创建"潍坊农田"配置
   - 地区: 山东省 潍坊市
   - 作物: 玉米
2. 创建"青岛农田"配置
   - 地区: 山东省 青岛市
   - 作物: 小麦

**好处**:
- 天气预报会自动使用对应地区
- 病害诊断会考虑当地气候特点

### 3. 为不同用途创建配置

**场景**: 有时需要诊断,有时需要查询价格

**操作**:
1. 创建"诊断配置"
   - 启用Skills: 病害识别、施肥计算
2. 创建"市场配置"
   - 启用Skills: 价格查询、天气预报

**好处**:
- Skills快捷栏只显示相关的按钮
- 界面更简洁

### 4. 快速复制和调整

**场景**: 新建配置时不想从头开始

**操作**:
1. 找到最接近的配置
2. 点击"复制"
3. 重命名为新配置
4. 修改需要调整的部分

**好处**:
- 节省配置时间
- 保持配置的一致性

## 🎨 界面说明

### 配置选择器
- 位置: Agent设置顶部
- 显示: 当前配置名称
- 点击: 打开配置列表下拉框

### 管理按钮
- 位置: 配置选择器右侧
- 图标: ⚙️ 设置图标
- 点击: 打开配置管理弹窗

### 配置列表
- 显示所有配置
- ⭐图标表示默认配置
- [当前使用]标记正在使用的配置
- 每个配置显示简要信息

### 操作按钮
- **切换**: 切换到该配置
- **编辑**图标: 重命名配置
- **复制**图标: 复制配置
- **删除**图标: 删除配置(至少保留1个)

## 🔑 技术实现

### Store状态管理

```javascript
import { useAgentConfigStore } from '@/stores/agentConfig'

const agentConfigStore = useAgentConfigStore()

// 获取所有配置
await agentConfigStore.fetchConfigs()

// 获取配置列表
const configs = agentConfigStore.configs

// 获取当前配置
const currentConfig = agentConfigStore.currentConfig

// 切换配置
await agentConfigStore.switchConfig(configId)

// 创建配置
await agentConfigStore.createConfig(configData)

// 更新配置
await agentConfigStore.updateConfig(configId, configData)

// 删除配置
await agentConfigStore.deleteConfig(configId)

// 复制配置
await agentConfigStore.duplicate(configId)

// 设置默认配置
await agentConfigStore.setDefault(configId)
```

### 数据结构

```javascript
{
  id: 'config-123',
  name: '玉米专用配置',
  description: '专门用于玉米种植的配置',
  isDefault: false,

  // 农业先验知识
  province: '山东省',
  city: '潍坊市',
  cropTypes: ['corn'],
  growthStage: 'growing',

  // 功能开关
  enableImageAnalysis: true,
  enableFieldManagement: true,
  enablePesticideAdvice: true,

  // 高级选项
  customPrompt: '你是一个专注于玉米种植的专家...',

  // Skills配置
  enabledSkillIds: ['disease-recognition', 'fertilizer-calculator'],

  // 系统字段(后端兼容)
  systemPrompt: { ... },
  agentConfig: { ... }
}
```

## 📊 配置持久化

配置数据会:
1. **本地存储**: 保存在localStorage中
2. **服务器同步**: 通过API同步到服务器
3. **自动加载**: 启动时自动加载上次使用的配置

## ⚠️ 注意事项

1. **至少保留一个配置**: 系统不允许删除最后一个配置
2. **配置命名**: 建议使用有意义的名称,如"玉米配置"而不是"配置1"
3. **配置同步**: 修改配置后记得点击"保存设置"
4. **切换配置**: 切换配置后,聊天上下文不会改变,但新消息会使用新配置

## 🎯 最佳实践

### 1. 合理命名
- ✅ 好的命名: "玉米生长期配置"、"潍坊农田"、"诊断专用"
- ❌ 不好的命名: "配置1"、"test"、"新配置"

### 2. 添加说明
在配置说明中写明:
- 这个配置用于什么场景
- 特殊的设置说明
- 注意事项

### 3. 定期整理
- 删除不再使用的配置
- 合并相似的配置
- 更新过时的配置

### 4. 备份重要配置
使用"复制"功能为重要配置创建备份。

## 📞 常见问题

### Q: 最多可以创建多少个配置?
A: 没有数量限制,但建议保持在5-10个以内,便于管理。

### Q: 删除配置后可以恢复吗?
A: 不能,删除操作不可逆,删除前请确认。

### Q: 切换配置会影响正在进行的聊天吗?
A: 不会影响已发送的消息,但新消息会使用新配置的设置。

### Q: 配置会自动同步吗?
A: 会,修改并保存后会自动同步到服务器。

---

**更新时间**: 2026-02-09
**版本**: v1.0
